#ifndef DATE_H
#define DATE_H

//header files decalre no functions

class Date
{
	
	private:
		int month;
		int day;
		int year;
	
	public:
		Date(int month, int day, int year);//prototype because has no body
		
		void display();
	
};

#endif
